### Web App (React)

```bash
# Start in dev mode
just dev-web

# Or manually:
cd web-app
npm install
npm run dev

# Build for production:
just build-web
# Output: web-app/dist/
```

**Dev URL:** `http://localhost:5173` (default Vite port)
