# Local development services
# Usage: docker compose up -d

services:
  # Placeholder for future database service
  # db:
  #   image: postgres:17
  #   environment:
  #     POSTGRES_USER: ml_mindmap
  #     POSTGRES_PASSWORD: ml_mindmap_dev
  #     POSTGRES_DB: ml_mindmap
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - docker-data/postgres:/var/lib/postgresql/data

  backend:
    build:
      context: .
      dockerfile: backend-service/Dockerfile
    ports:
      - "3000:3000"
    # environment:
    #   DATABASE_URL: postgres://ml_mindmap:ml_mindmap_dev@db:5432/ml_mindmap
    # depends_on:
    #   - db
