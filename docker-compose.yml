# Local development services
# Usage: docker compose up -d

services:
  # Placeholder for future database service
  # db:
  #   image: postgres:17
  #   environment:
  #     POSTGRES_USER: mindmap
  #     POSTGRES_PASSWORD: mindmap_dev
  #     POSTGRES_DB: mindmap
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - docker-data/postgres:/var/lib/postgresql/data

  backend:
    build:
      context: .
      dockerfile: backend-service/Dockerfile
    ports:
      - "3000:3000"
    # environment:
    #   DATABASE_URL: postgres://mindmap:mindmap_dev@db:5432/mindmap
    # depends_on:
    #   - db
