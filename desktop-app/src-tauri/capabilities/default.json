{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "description": "Default capabilities for the desktop app",
    "windows": ["main"],
    "permissions": [
        "dialog:default",
        "upload:default",
        "fs:default",
        "core:default"
    ]
}
